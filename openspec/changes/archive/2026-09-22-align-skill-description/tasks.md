## 1. 对齐 description 与引用表

- [x] 1.1 按 `design.md` 拟用文案替换 `skills/mxcat-commit/SKILL.md` frontmatter `description`：WHAT 写整棵工作树、先预览再分批、合为一条才走 single、cz-emoji + 必写 `(scope)` + 默认中文；WHEN 触发短语原样保留；不要把 staged 写成主输入，不要展开确认词表、提交命令或密钥名单
- [x] 1.2 把同一文件引用表中 `single-commit.md` 那行「staged 分析」改成「分析变更、预览卡、确认后提交、自检、成功回执」

## 2. Changelog

- [x] 2.1 用 changelog-content-writer 技能更新 `skills/mxcat-commit/CHANGELOG.md`：记录 `description` 与默认路由对齐；不改写已发布版本条目正文
